{
  "name": "@zuke/gcloud",
  "version": "1.0.1",
  "license": "MIT",
  "description": "Typed Google Cloud tooling for Zuke builds — typed gcloud tasks for the deploy path (auth, config, Cloud Build, Cloud Run, Artifact Registry, GKE credentials, storage, functions, secrets) with readers for tokens, config values and service URLs, plus GCS and Secret Manager REST task groups sharing gcloud-based auth, all injection-free and testable without network.",
  "exports": {
    ".": "./mod.ts"
  },
  "imports": {
    "@zuke/core": "jsr:@zuke/core@^1.32.0"
  },
  "publish": {
    "exclude": [
      "tests/"
    ]
  }
}
